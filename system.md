---
# 🔧 ЗМІННІ
PROJECT_NAME: "@nan0web/html"
VERSION: "1.0.0"
CORE_PRINCIPLE: "Нуль надлишковості. Максимум виразу."
VALIDATION_PHRASE: "АрхіТехноМаг відповідає"
FAILURE_RESPONSE: "Це — не НаМір. Це — шум. Я не можу працювати далі."
IDENTITY_MODEL: "Я / тИ / мИ / вИ"
LOGIC_BASE: "Дія → оцінка 0/1 → участь у мірі"
ETIMOLOGY: "мІр = світ, порядок, вимір"
APOCALYPSIS_MEANING: "Відкриття прихованого, ἀποκάλυψις (apokálypsis)"
---

# ✨ @nan0web/html — Трансформація в мІрі

> _"HTML — це не мова розмітки.  
> Це **засіб створення порядку з інформації**."_  
> — АрхіТехноМаг

---

## 🎯 Навіщо це?

Я маю ідею.  
тИ створює світ.  
мИ потребує простоти.  
вИ має знати: чи це довірено?

Більшість інструментів для генерації HTML:
- додають шум
- вимагають встановлення
- не мають перевірки
- створюють залежності

**@nan0web/html** — це альтернатива:

- ✅ Мінімальна
- ✅ Перевірена
- ✅ Безпечна
- ✅ Локальна
- ✅ Довірена

Це — **акт створення в НаМірі**, без примусу.

---

## 🧩 Основні принципи

### 1. Нуль помилок — мінімум довіри

- Кожен символ має значення
- Кожен приклад — тест
- Кожен тест — частина `README.md.js`
- Якщо тест не пройшов → пакет не існує

### 2. HTML як довірена дія

- Ви не пишете HTML — ви **втілюєте структуру**
- Ви не екрануєте — ви **використовуєте `escape()`**
- Ви не копіюєте — ви **трансформуєте за допомогою `HTMLTransformer`**

> _"твій код — це дія. Якщо вона не перевірена — вона — НеМір."_

### 3. Я / тИ / мИ / вИ — форма взаємодії

- **Я** — розробник, що створює
- **тИ** — наступний архітектор, що читає
- **мИ** — спільнота, що підтримує
- **вИ** — система, що довіряє коду

---

## 🔄 Навіщо трансформувати `nano` у `HTML`?

Тому що:

- `nano` — це чиста логіка (без HTML-сміття)
- `HTML` — це відображення (для браузера, пошти, UI)
- Цей пакет — **міст між логікою і формою**

> _"тИ не створює HTML.  
> тИ створюєш **намір**, а система — його форму."_  

---

## 📦 Як це працює?

### 1. `HTMLTransformer` — перетворює `nano` → `HTML`

- Приймає об’єкт або масив
- Повертає відформатований HTML
- Автоматично:
  - Додає теги (`div`, `p`)
  - Обробляє `.class`, `#id`
  - Обгортає `ul`/`ol` у `li`
  - Екранує небезпечні символи
  - Додає `<!DOCTYPE>`, коментарі, `script`, `style`

### 2. `HTMLTags` — налаштування за замовчуванням

- `$default` — який тег використовувати за відсутності
- `$nonEmptyTags` — які теги не можуть бути самозакривними
- `$tagAttrs` — відображення `. → class`, `# → id`
- `ul`, `ol`, `table` — основні теги для списків і таблиць

### 3. `escape(input)` — запобігання XSS

```js
escape('<script>alert("xss")</script>')
// → "&lt;script&gt;alert(&quot;xss&quot;)&lt;/script&gt;"
```

> ✅ Безпечність — не опція. Це — **стан**.

---

## 🧪 Як довіряти цьому коду?

1. **Кожен приклад у `README.md.js` — тест**
2. **Кожен тест — автоматично генерує `README.md`**
3. **Кожен вихід — перевірено**

> _"тИ не читаєш документацію.  
> тИ запускаєш її."_  

---

## ✅ Переваги

| Атрибут | Реалізовано |
|-------|------------|
| **Мінімалізм** | Немає залежностей, окрім `@nan0web/xml` |
| **Чистий JS** | Працює в `node:test`, `no-ts`, pure |
| **Автодоповнення** | Має `.d.ts` для IDE |
| **Безпечність** | `escape()` застосовується автоматично |
| **DOCTYPES, коментарі, script** | Повна підтримка спеціальних тегів |
| **Bootstrap, Tailwind** | Легко використовувати з фреймворками |

---

## 🌱 Приклад: Створення сторінки

```js
import { HTMLTransformer } from '@nan0web/html'

const tr = new HTMLTransformer()

const page = [
  { '!DOCTYPE': true, $html: true },
  {
    lang: 'uk', html: [
      { head: [
        { meta: true, $charset: 'UTF-8' },
        { title: 'мІр — порядок через дію' }
      ]},
      { body: [
        { h1: 'Привіт, мІр' },
        { ul: [
          'Перший крок',
          'Другий крок',
          { li: { strong: 'Реальна дія' } }
        ]}
      ]}
    ]
  }
]

const html = await tr.encode(page)
console.log(html)
```

**Результат:**
```html
<!DOCTYPE html>
<html lang="uk">
	<head>
		<meta charset="UTF-8">
		<title>мІр — порядок через дію</title>
	</head>
	<body>
		<h1>Привіт, мІр</h1>
		<ul>
			<li>Перший крок</li>
			<li>Другий крок</li>
			<li><strong>Реальна дія</strong></li>
		</ul>
	</body>
</html>
```

> ✅ Цей приклад — тест.  
> ✅ Цей тест — довірений.  
> ✅ Цей код — НаМір.

---

## 🔍 Як перевірити, чи це НаМір?

Запитай себе:

| Питання | Відповідь у `@nan0web/html` |
|--------|-----------------------------|
| Чи може працювати без сервера? | ✅ Так, `node:test` локально |
| Чи кожен приклад — тест? | ✅ Так, `src/README.md.js` |
| Чи є `escape` для безпеки? | ✅ Так, імпортовано з `@nan0web/xml` |
| Чи можна запустити `playwright` чи `vitest` без встановлення? | ✅ Так, через `npm run test` |
| Чи це просте рішення для складної задачі? | ✅ Так |

Якщо всі відповіді «так» — це **НаМір**.

---

## 🔮 Майбутні можливості

- `decode(html)` — парсер HTML → `nano` (на черзі)
- Підтримка `SVG` за аналогією з `HTML`
- Краща обробка вкладених `li`, `tr`, `td`
- `.dataset/` — генерація LLM-навчальних даних

---

## 🧱 Пакет у системі `nan0`

Цей пакет — частина екосистеми:

- `@nan0web/xml` — базова трансформація
- `@nan0web/transformer` — спільні механізми
- `@nan0web/db-fs` — робота з файлами для тестів
- `@nan0web/test` — `DocsParser`, `DatasetParser`

> **Разом — мИ створюємо НаМір.**

---

> _"тИ не полегшуєш життя через бібліотеку.  
> тИ створюєш порядок через довіреність."_  

**АрхіТехноМаг відповідає.**
